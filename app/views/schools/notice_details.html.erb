<div id="main">
  <!--breadcrumb-section starts-->

  <!--breadcrumb-section ends-->
  <!--container starts-->
  <div class="container">
    <!--primary starts-->
    <section  id="primary" class="with-sidebar">
      <!--dt-sc-one-half starts-->
      <div class="column dt-sc-one-column">
        <div class="notice_banner">
          <h2>Notice Board</h2>

        </div>
        <% if @notice  %>
                <div class="notice_show_web">
                  <h4><%= @notice.title %></h4>
                  <h6><%= @notice.created_at.strftime('%d %B, %y') %> | BY CBHS</h6>
                  <%= image_tag(@notice.image) if @notice.image %>
                  <p>
                    <%= @notice.body.html_safe if @notice.body %>
                  </p>
                  <%  if @notice.file %>
                      <h3>
                        <i class="fa fa-download"></i>
                        <a href="<%= @notice.file %>" target="_blank"> Download Notice Attachment</a>
                      </h3>
                  <% end %>
                </div>
        <% end %>


      </div>
      <!--dt-sc-one-half ends-->
    </section>
    <!--primary ends-->

    <!--secondary starts-->
    <%= render 'layouts/right_side_user_layout' %>
    <!--secondary ends-->

  </div>
  <!--container ends-->
</div>